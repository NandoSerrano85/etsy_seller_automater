/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add your API host domain here for production
    // domains: ['your-api-host.com'],
    domains: []
  }
}

module.exports = nextConfig